using DatabaseConnection;
using System;
using System.Collections.Generic;
using System.Text;

namespace FlexApp.User
{
    static class UserSearch
    {

        public static Movie Search(string input, bool[] options)
        {
            // ct.Movies.Where(m=>m.Title == input);

            return new Movie();
        }

    }
}
